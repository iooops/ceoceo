extends ../../layouts/default

include ../../mixins/organisation

block intro
	.page-intro: .container
		h1 Organizations
		p.lead The organizations and people who are part of CEO. Should you be here? Join us!
		//- .buttons
			if !user
				a(href='/join').btn.btn-primary Join CEO
			a(href='/members').btn.btn-default View Members
			a(href='/members/mentors').btn.btn-default View Mentors

block content
	.page-content: .container
		.organisations
			if organisations.length
				each org, i in organisations
					if i
						hr
					+organisation(org)
			else
				h3.text-muted There are no organizations yet.
